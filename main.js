const app = Vue.createApp({
    //options object - mandatory, at least empty object
    //data: function () { <--longhand version, ES6 shorthand below
    data() {
        return {
            product: 'Socks',
            description: 'They are socks!'
        }
    }
})


